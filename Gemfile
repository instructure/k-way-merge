source "http://rubygems.org"

gem "pqueue"

group :test do
  gem 'rspec', :require => "spec"
  if RUBY_VERSION >= "1.9."
    gem 'debugger',     '1.1.3'
  else
    gem 'ruby-debug',   '0.10.4'
  end
end
